# 在同事电脑上打开 SeedLandV（保姆级）

> 场景：**后端跑在你这台机器**（IP `192.168.1.19`，已在运行），同事电脑装上桌面端、连过来用。
> 前提：两台电脑连**同一个路由器 / 同一个 WiFi**。

---

## 第 0 步（在「你这台」做一次）：放行防火墙 ⭐ 必做

同事连不上，99% 是这一步没做。端口已经在监听，只是被 Windows 防火墙挡了。

1. 开始菜单搜 **PowerShell** → **右键 → 以管理员身份运行**。
2. 粘这条回车：

   ```powershell
   New-NetFirewallRule -DisplayName "SeedLandV API 8000" -Direction Inbound -LocalPort 8000 -Protocol TCP -Action Allow
   ```

3. 看到返回一段规则信息就成了。（这步只需做一次。）

> 顺带确认后端在跑：普通 PowerShell 里 `docker compose -f docker-compose.prod.yml --env-file .env.docker ps`，看到 api/postgres/redis 是 healthy 即可。这台机别关机、别退出 Docker Desktop，同事才能连。

---

## 在「同事电脑」上做

### A. 装桌面端

1. 把安装包 **`SeedLandV-0.1.0-Setup.exe`**（约 79MB）发给同事（微信传文件 / U盘 / 共享文件夹都行）。
2. 同事双击安装。
3. 弹蓝色 **「Windows 已保护你的电脑」** → 点 **「更多信息」→「仍要运行」**（没买签名证书，正常现象），按提示装完。

### B. 让它连到你的后端（最关键一步）

桌面端默认连它自己的 `localhost`，必须改成**你这台机的地址**：

1. 右键桌面的 **SeedLand V** 快捷方式 → **「打开文件所在位置」**，进到 `SeedLandV.exe` 所在的文件夹。
2. 在**这个文件夹空白处** → 按住 **Shift + 右键** → **「在终端中打开」**（或「在此处打开 PowerShell」），粘这条回车：

   ```powershell
   Set-Content -Path .\.env -Value "SEEDLANDV_API_URL=http://192.168.1.19:8000" -Encoding utf8
   ```

   > 这会在 exe 旁边生成一个 `.env` 文件，告诉 app 后端在 `192.168.1.19:8000`。
   > 如果你这台机的 IP 变了（重启后可能变），把上面的 `192.168.1.19` 换成新 IP 再跑一次。

3. **关掉 SeedLandV 再重新打开**（让它重新读配置）。

### C. 登录

- 工号 **B001**　密码 **Tongshi@2026**（已经给同事建好的账号）
- 能进到主界面 = **连通成功** 🎉

---

## 连不上？按顺序排查

| 检查 | 怎么弄 |
|---|---|
| ① 防火墙放行了吗 | 回「第 0 步」，必须管理员身份跑那条命令 |
| ② 两台在同一网络吗 | 同事电脑连的 WiFi/网线，要和你这台同一个路由器；连了 VPN 先断开 |
| ③ 能 ping 通吗 | 同事 PowerShell 跑 `ping 192.168.1.19`，不通就是网络/防火墙问题 |
| ④ 后端在跑吗 | 你这台 `docker compose -f docker-compose.prod.yml --env-file .env.docker ps` 全 healthy |
| ⑤ `.env` 写对了吗 | 必须有 `http://`、IP 正确、端口 `:8000`；改完要重开 app |
| ⑥ IP 是不是变了 | 你这台 `ipconfig` 看 WLAN 的 IPv4 还是不是 192.168.1.19，变了就更新同事的 `.env` |

> 快速验证后端是否可达：同事浏览器打开 `http://192.168.1.19:8000/healthz`，看到 `{"status":"ok"}` 就说明网络通了，问题只在 app 配置。

---

## ⚠️ 重要：现在能登录，但还**不能生成视频**

后端的 **火山 TOS** 和 **方舟 Ark Key** 你还没配，所以：

- ✅ 同事能装、能打开、能登录、能看界面、能连上后端 —— 这条链路现在就能验证。
- ❌ 点「生成」会失败 —— 要等你把 TOS（填 `.env.docker`）和 Ark Key（登录后弹窗里填）搞定。

这两样怎么弄见 [保姆级部署教程.md](保姆级部署教程.md) 的步骤一 / 步骤四。

---

## 想再加人

同样方式给每个人发 exe + 建 `.env` 即可。每人单独账号（在你这台后端机跑）：

```powershell
docker compose -f docker-compose.prod.yml --env-file .env.docker run --rm api `
  python -m scripts.create_initial_admin --employee-id C001 --password "改个密码" --display-name "张三"
```
