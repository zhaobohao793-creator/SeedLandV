# SeedLandV 保姆级部署教程（局域网版）

> 适用场景：**后端跑在一台本地机器（叫它「后端机」）**，你和同事的电脑在**同一个局域网/路由器**下，装上桌面端连过去用。不涉及公网域名/HTTPS。

---

## 0. 最终会变成这样

```
   ┌─────────────┐          局域网          ┌──────────────────────────┐
   │ 你/同事的电脑 │  ───────────────────▶   │  后端机 (192.168.x.x)      │
   │ SeedLandV.exe│   http://192.168.x.x:8000│  Docker: api+worker+beat  │
   └─────────────┘                          │        +postgres+redis    │
                                            └──────────┬───────────────┘
                                                       │ 上网调用
                                            火山 TOS(存素材/产物) + 火山方舟 Ark(生成)
```

**你要搞定 3 件事**：① 开通火山 TOS 拿到 AK/SK；② 在后端机用 Docker 把后端跑起来；③ 桌面端填上后端机的局域网地址。

> 💡 **偷懒选项**：后端其实也能直接跑在「现在这台已经构建好镜像的机器」上，别的电脑连这台的 IP 就行，省去搬文件那一步。如果你愿意，就把下面「后端机」理解成「这台机」，跳过步骤二的"搬文件"。

---

## 步骤一　开通火山 TOS（对象存储）— 拿 AK / SK / 桶名

TOS 是用来存上传素材和生成产物的，必须有。

1. 浏览器打开 **https://console.volcengine.com** ，注册/登录，完成**实名认证**（个人/企业都行，不认证用不了）。
2. 顶部搜索框搜 **「对象存储 TOS」**，进去点**开通服务**（免费开通，按用量付费，量小几乎不要钱）。
3. **建桶（Bucket）**：左侧「桶列表」→「创建桶」：
   - 桶名：`seedlandv-prod`（自己起，全局唯一，记下来）
   - 地域：**华北2（北京）** → 对应 region `cn-beijing`
   - 读写权限：**私有**
   - 其他默认，创建。
4. **建子账号拿钥匙（AK/SK）**：顶部搜 **「访问控制 / IAM」** →「用户」→「新建用户」：
   - 勾选 **「编程访问 / 程序访问」**（这样才会给你 AccessKey）
   - 创建后给它授权：找 **`TOSFullAccess`** 策略勾上（保守起见可只授权到刚才那个桶）
   - 创建完成会显示 **AccessKeyId(AK)** 和 **SecretAccessKey(SK)** —— **SK 只显示这一次，立刻复制保存！** 丢了只能重置。
5. 现在你手里应该有这 5 个值（记在记事本里待会儿用）：

   | 名字 | 例子 |
   |---|---|
   | AK（AccessKeyId） | `AKLT****************` |
   | SK（SecretAccessKey） | `****************` |
   | 桶名（Bucket） | `seedlandv-prod` |
   | 区域（Region） | `cn-beijing` |
   | Endpoint | `tos-cn-beijing.volces.com` |

---

## 步骤二　在后端机用 Docker 把后端跑起来

### 2.1 装 Docker

后端机如果还没装 Docker：

- **Windows**：装 **Docker Desktop** → https://www.docker.com/products/docker-desktop ，装完**打开它**，等右下角小鲸鱼图标不再转圈（引擎就绪）。
- （Linux 服务器则装 Docker Engine，命令不同，这里按 Windows 走。）

### 2.2 把后端文件搬到后端机

后端只需要这几样，复制到后端机的一个文件夹里（比如 `D:\SeedLandV`），**保持这个目录结构**：

```
D:\SeedLandV\
  ├─ docker-compose.prod.yml
  ├─ .env.docker              ← 里面密钥我已生成好，待会儿补 TOS
  └─ server\                  ← 整个 server 文件夹都要
```

> 用 U 盘 / 共享文件夹 / 微信传文件都行。`node_modules`、`dist`、`out` 这些**不用**拷，太大也没用。
> 偷懒选项（后端就跑这台）则无需搬，直接用当前目录。

### 2.3 把 TOS 填进 .env.docker

用记事本打开 `.env.docker`，把步骤一拿到的三项填上（数据库密码/JWT/Fernet 我已经随机生成好了，**别动**）：

```ini
VOLC_TOS_AK=这里填你的AK
VOLC_TOS_SK=这里填你的SK
VOLC_TOS_BUCKET=seedlandv-prod
VOLC_TOS_REGION=cn-beijing
VOLC_TOS_ENDPOINT=tos-cn-beijing.volces.com
```

保存。

### 2.4 起服务（一条命令）

在后端机打开 **PowerShell**，进到那个文件夹，执行：

```powershell
cd D:\SeedLandV
docker compose -f docker-compose.prod.yml --env-file .env.docker up -d --build
```

第一次要构建镜像 + 下载，**等几分钟**。看到一堆 `Started` / `Healthy` 就差不多了。

> ⚠️ 之后**每一条** `docker compose` 命令都要带 `--env-file .env.docker`，否则会报 “required variable … is missing”。嫌烦可以先 `$env:COMPOSE_ENV_FILES=".env.docker"`（仅当前窗口有效）。

### 2.5 验证后端起来了

```powershell
docker compose -f docker-compose.prod.yml --env-file .env.docker ps
curl http://localhost:8000/readyz
```

- `ps` 里 `api/postgres/redis` 应是 **healthy**，`worker/beat` 是 **running**。
- `/readyz` 应返回 `db ok / redis ok / tos ok`，**三个都 ok** 才算齐。
  - 如果 `tos` 报错 → 多半是 AK/SK/桶名/区域填错了，回步骤 2.3 改完 `docker compose ... up -d` 再试。

### 2.6 建第一个管理员账号

库是空的，先建个登录账号（工作室名/工号/密码自己定）：

```powershell
docker compose -f docker-compose.prod.yml --env-file .env.docker run --rm api `
  python -m scripts.create_initial_admin `
  --workshop "我的工作室" --employee-id A001 --password "你的强密码"
```

看到 `created admin: ... employee_id='A001'` 就成了。

---

## 步骤三　找到后端机的局域网地址 + 放行端口

### 3.1 查 IP

后端机 PowerShell 里跑：

```powershell
ipconfig
```

找你**正在用的网卡**（Wi‑Fi 或 以太网）下的 **「IPv4 地址」**，形如 `192.168.1.50`。这就是别的电脑要连的地址，记下来。

### 3.2 放行防火墙 8000 端口

用**管理员身份**打开 PowerShell（开始菜单右键 PowerShell → 以管理员运行），执行：

```powershell
New-NetFirewallRule -DisplayName "SeedLandV API 8000" -Direction Inbound -LocalPort 8000 -Protocol TCP -Action Allow
```

（这样同局域网其他电脑才连得进来。）

---

## 步骤四　桌面端装上并连到后端

在**每个要用的人**的电脑上：

### 4.1 安装

1. 把 `dist\SeedLandV-0.1.0-Setup.exe` 发给对方，双击安装。
2. 弹蓝色 **「Windows 已保护你的电脑」** → 点 **「更多信息」→「仍要运行」**（因为没买代码签名证书，正常现象）。
3. 按提示装完。

### 4.2 告诉 app 后端在哪（关键一步）

桌面端默认连 `localhost:8000`，要改成后端机的地址：

1. 右键桌面的 **SeedLand V** 快捷方式 →「打开文件所在位置」，找到 `SeedLandV.exe` 所在文件夹。
2. 在**这个文件夹**里新建一个文件名就叫 **`.env`** 的文本文件，内容一行：
   ```ini
   SEEDLANDV_API_URL=http://192.168.1.50:8000
   ```
   把 `192.168.1.50` 换成步骤 3.1 查到的后端机 IP。

   > Windows 资源管理器不好直接建 `.env`？用这招：在该文件夹空白处 Shift+右键 →「在终端中打开」，粘：
   > ```powershell
   > Set-Content -Path .\.env -Value "SEEDLANDV_API_URL=http://192.168.1.50:8000" -Encoding utf8
   > ```
3. 关掉再重新打开 SeedLandV（让它重新读配置）。

### 4.3 登录 + 填 Ark Key + 出片

1. 用步骤 2.6 建的 **工号 A001 / 密码** 登录。
2. 登录后会自动弹 **「配置 Ark API Key」**：
   - 点窗里的链接去 **console.volcengine.com/ark** → 「API Key 管理」新建一个，复制（`ark_` 开头）。
   - 粘进去保存。（这个 Key 每个工作室填一次，之后所有人共用，加密存后端。）
3. 选个模式（文生视频最简单），写提示词，点生成。任务卡片转起来→出视频，就**全通了** 🎉

---

## 常见问题排查

| 现象 | 解决 |
|---|---|
| 桌面端一直转/连不上、登录报网络错 | ① 后端机 `docker compose ... ps` 是否都在跑；② `.env` 里 IP 对不对、有没有 `http://` 和 `:8000`；③ 防火墙是否放行（步骤 3.2）；④ 两台电脑是否同一个路由器/网段 |
| `/readyz` 里 tos 报错 | AK/SK/桶名/区域填错，或桶不在 cn-beijing。改 `.env.docker` 后 `up -d` 重来 |
| 登录提示账号/密码错 | 账号没建或工号/密码不对，回步骤 2.6 重建（换个工号即可） |
| 生成时报 Ark 相关错 | Ark Key 没填/失效/余额不足，点头像或设置重设 Key；去方舟控制台确认 Key 有效、模型已开通 |
| 后端机重启后要不要手动起 | 不用。容器是 `restart: unless-stopped`，开机自动起；把 Docker Desktop 设成「开机启动」即可 |
| 想看后端日志 | `docker compose -f docker-compose.prod.yml --env-file .env.docker logs -f api worker` |
| 改了代码/换了机器 | 在后端机重跑 `docker compose -f docker-compose.prod.yml --env-file .env.docker up -d --build` |

---

## 日常运维速记（在后端机）

```powershell
# 状态 / 日志
docker compose -f docker-compose.prod.yml --env-file .env.docker ps
docker compose -f docker-compose.prod.yml --env-file .env.docker logs -f worker

# 备份数据库（订单/账号都在这）
docker compose -f docker-compose.prod.yml --env-file .env.docker exec postgres `
  pg_dump -U seedlandv seedlandv > backup.sql

# 停 / 彻底删（-v 连数据卷一起删，慎用）
docker compose -f docker-compose.prod.yml --env-file .env.docker down
```

更多细节见 [DEPLOY.md](DEPLOY.md)。
